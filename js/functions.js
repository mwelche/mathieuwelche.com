/* functions baby */
$(document).ready(function() {
	/* $('.resume .skills li').draggable({
		connectToSortable: "#skills-list",
		containment: "parent",
		helper: "clone",
		revert: "invalid"
	});
	$('#skills-list').sortable({
		revert: true
	}); */
	
	/*
	$('#skills-list').gridly({
		base: 60, // px 
		gutter: 20, // px
		columns: 12
	});
	*/
	$('.add-skill').hide();
});